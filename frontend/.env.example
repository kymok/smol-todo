# Copy to .env.local (git-ignored) and tweak for local development.

# Show the debug title-bar / window-drag-strip background overlay.
# "1" enables it; anything else (or unset) hides it. No effect in production
# builds — the overlay is always stripped from `vite build` output.
VITE_DEBUG_TITLEBAR=0
