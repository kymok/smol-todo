# Styling

- The icon library to use is [Lucide](https://lucide.dev/) (`lucide-react`).
- The styling library to use is [Tailwind CSS](https://tailwindcss.com/).
- Keep Tailwind classes confined within components; do not write them directly in screens.
